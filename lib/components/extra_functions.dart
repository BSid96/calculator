import 'package:flutter/material.dart';

class ExtraFunctions extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Color(0xFF375DCA),
      child: Text("extra functions"),
    );
  }
}
