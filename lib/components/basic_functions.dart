import 'package:flutter/material.dart';

class BasicFunctions extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Color(0xFF202125),
      child: Text("basic functions"),
    );
  }
}
